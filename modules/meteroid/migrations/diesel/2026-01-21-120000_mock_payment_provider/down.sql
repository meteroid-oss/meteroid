-- Note: PostgreSQL does not support removing enum values directly.
-- The MOCK value will remain in the enum but won't be used in production.
-- If needed, the enum would need to be recreated without the MOCK value.
SELECT 1;
