-- Note: PostgreSQL does not support removing enum values directly.
-- The STANCER value will remain in the enum but won't be used in production.
-- If needed, the enum would need to be recreated without the STANCER value.
SELECT 1;
