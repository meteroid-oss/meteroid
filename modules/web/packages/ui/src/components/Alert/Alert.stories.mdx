import { Meta, Story } from '@storybook/addon-docs'
import { CommandLineIcon, ExclamationCircleIcon } from '@heroicons/react/24/solid'
import { Alert, AlertDescription, AlertTitle } from './Alert'

<Meta title="Alert" component={Alert} />

# Alert

<Alert title="Info">You can add components to your app using the cli.</Alert>

<Alert variant="danger" title="Attention">
  You can add components to your app using the cli.
</Alert>
