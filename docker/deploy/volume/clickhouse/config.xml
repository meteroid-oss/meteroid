<clickhouse>
  <!-- Embedded Keeper (single node, no separate ZooKeeper needed) -->
  <keeper_server>
    <tcp_port>9181</tcp_port>
    <server_id>1</server_id>
    <log_storage_path>/var/lib/clickhouse/coordination/log</log_storage_path>
    <snapshot_storage_path>/var/lib/clickhouse/coordination/snapshots</snapshot_storage_path>
    <coordination_settings>
      <operation_timeout_ms>10000</operation_timeout_ms>
      <session_timeout_ms>30000</session_timeout_ms>
      <raft_logs_level>warning</raft_logs_level>
    </coordination_settings>
    <raft_configuration>
      <server>
        <id>1</id>
        <hostname>localhost</hostname>
        <port>9234</port>
      </server>
    </raft_configuration>
  </keeper_server>

  <!-- Point ClickHouse at the embedded Keeper -->
  <zookeeper>
    <node>
      <host>localhost</host>
      <port>9181</port>
    </node>
  </zookeeper>

  <!-- Single-node cluster -->
  <remote_servers>
    <meteroid>
      <shard>
        <replica>
          <host>localhost</host>
          <port>9000</port>
        </replica>
      </shard>
    </meteroid>
  </remote_servers>

  <!-- Macros substituted in ReplicatedMergeTree paths and ON CLUSTER DDL -->
  <macros>
    <cluster>meteroid</cluster>
    <shard>1</shard>
    <replica>replica-1</replica>
  </macros>
</clickhouse>
